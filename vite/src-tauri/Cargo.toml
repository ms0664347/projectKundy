[package]
name = "app"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
license = ""
repository = ""
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.5.1", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"

# 必備 Tauri Core
tauri = { version = "2.9.2", features = [] }

# 你有使用的 Plugin
tauri-plugin-log = "2"
tauri-plugin-fs = "2"

# 🚀 你最需要修的地方
# 你的寫法是錯的 reqwest = "0.12.24"
# 必須改成下面這個 ↓↓↓  才能使用 .json(...)
reqwest = { version = "0.12", features = ["json"] }

# Tauri + reqwest 一定要 async runtime
tokio = { version = "1.48.0", features = ["rt-multi-thread", "macros"] }
dotenvy = "0.15.7"
